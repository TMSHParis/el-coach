// Constantes des photos de séance. Module neutre (pas de "use client"/"use
// server") : importé aussi bien par le composant d'upload côté navigateur que
// par les Server Actions qui bornent ce qui est enregistré.

/** 2 photos maximum par séance. */
export const MAX_SESSION_PHOTOS = 2;
